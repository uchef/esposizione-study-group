# OpenAPI server stub

Lightweight Express server that implements a mock/proxy for the `openapi.yaml` endpoints.

Usage:

```bash
cd openapi-server
npm install
npm start
# then visit http://localhost:8080/John%203:16
```

This is intended as a local stub for development and testing only.
